name: llmlearner
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - seaborn
  - tqdm
  - jupyterlab
  - ipywidgets
  - pytorch=2.1
  - torchvision
  - torchaudio
  - pytorch-cuda=11.8  # GPU-enabled PyTorch build
  - sentencepiece
  - pip:
      - transformers
      - datasets
      - accelerate
      - bitsandbytes
      - evaluate
      - sacrebleu
      - bert-score
      - umap-learn
      - sae-lens
      - huggingface_hub
      - wandb
      - scipy
      - torchinfo
      - rich
      - pyarrow